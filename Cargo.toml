[workspace]
members = [
    "crates/apple-media",
    "crates/browser",
    "crates/browser-cef",
    "crates/core",
    "crates/agent",
    "crates/tools",
    "crates/llm",
    "crates/security",
    "crates/memory",
    "crates/experience",
    "crates/locale",
    "crates/dashboard",
    "crates/setup",
    "crates/config",
    "crates/channel-bridge",
    "crates/bootstrap",
    "crates/dashboard-ipc",
    "crates/relay-gateway",
]
exclude = ["crates/account-service"]
resolver = "2"

[workspace.package]
version = "0.41.6"
edition = "2021"
authors = ["anyCode Contributors"]
license = "MIT"
repository = "https://github.com/qingjiuzys/anycode"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.35", features = ["full"] }
async-trait = "0.1"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Database
sled = "0.34"
sqlx = { version = "0.7", features = ["sqlite", "runtime-tokio"] }

# HTTP/WebSocket
reqwest = { version = "0.11", features = ["json", "stream"] }
tokio-tungstenite = "0.21"
hyper = { version = "0.14", features = ["full"] }

# AI/ML (optional, for future local LLM support)
# candle-core = { version = "0.3", optional = true }

# Tools
walkdir = "2.4"
ignore = "0.4"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = "0.3"

# Caching
moka = { version = "0.12", features = ["future"] }

# Filesystem watch
notify = "6.1"

# Utils
uuid = { version = "1.6", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
cron = "0.15"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

# Local desktop/daemon iteration: skip LTO (release ~5min → ~1–2min link).
[profile.release-local]
inherits = "release"
lto = false
codegen-units = 16
opt-level = 3
strip = false

# 工作区统一 Clippy：抑制噪声类规则，保持 `cargo clippy` 零告警（CI 与本地一致）。
[workspace.lints.clippy]
too_many_arguments = "allow"
type_complexity = "allow"
borrowed_box = "allow"
module_inception = "allow"
await_holding_lock = "allow"
if_same_then_else = "allow"
assertions_on_constants = "allow"
field_reassign_with_default = "allow"
needless_range_loop = "allow"
while_let_loop = "allow"
unnecessary_get_then_check = "allow"
match_like_matches_macro = "allow"
nonminimal_bool = "allow"
manual_clamp = "allow"
manual_strip = "allow"

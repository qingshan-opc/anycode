anycode/
├── Cargo.toml                 # Rust workspace
├── .github/workflows/         # CI, docs publish, desktop release
├── README.md
├── WORKBENCH.md               # Digital Workbench notes
├── DESIGN.md                  # Product / brand contract
├── docs/                      # Maintainer docs, ADRs, roadmap
├── docs/user/                 # User docs (anycode.work/docs)
├── scripts/                   # Install/build/eval scripts
├── skills-starter/            # Bundled Agent Skills
├── apps/
│   └── anycode-desktop/       # Tauri shell; in-process dashboard + WebView
│
└── crates/
    ├── core/                  # Domain types + traits
    ├── agent/                 # AgentRuntime (LLM + tool loop)
    ├── tools/                 # Built-in tools, skills, MCP, workflows
    ├── llm/                   # LLM providers
    ├── security/              # Approval + policy
    ├── memory/                # Memory backends
    ├── bootstrap/             # initialize_runtime composition root
    ├── config/                # ~/.anycode/config.json schema
    ├── locale/                # Locale helpers
    ├── dashboard/             # Workbench HTTP + SQLite + embedded Agent
    ├── dashboard-ui/          # React/Vite UI (not a Cargo member; embedded at build)
    ├── dashboard-ipc/         # Approval / question / cancel file IPC
    ├── channel-bridge/        # anycode-daemon (scheduler only)
    ├── setup/                 # First-run setup helpers
    ├── browser/ / browser-cef/# Browser automation / CEF helper
    ├── apple-media/           # macOS Speech/OCR helpers
    ├── relay-gateway/         # Local relay
    ├── account-service/       # Cloud account API (workspace.exclude)
    ├── account-portal/        # Docs / account Vite SPA
    └── ops-portal/            # Ops Vite SPA

Removed (do not look for these):
- crates/cli, crates/channels, crates/onboard (terminal CLI + IM bridges)
- anycode-daemon wechat/telegram/discord bridge subcommands

Notes: `docs/` is maintainer + ADR; user-facing docs live under `docs/user/`.
`crates/dashboard-ui/` is Node/Vite, embedded by dashboard/desktop build scripts.
